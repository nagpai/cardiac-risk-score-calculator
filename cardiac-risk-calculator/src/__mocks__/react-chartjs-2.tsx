// Mock for react-chartjs-2
import React from 'react';

export const Bar = ({ data, options }: any) => (
  <div data-testid="bar-chart">Bar Chart Mock</div>
);

export const Doughnut = ({ data, options }: any) => (
  <div data-testid="doughnut-chart">Doughnut Chart Mock</div>
);